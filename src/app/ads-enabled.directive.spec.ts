import { AdsEnabledDirective } from './ads-enabled.directive';

describe('AdsEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new AdsEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
