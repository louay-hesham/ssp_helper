import { EventClickDirective } from './event-click.directive';

describe('EventClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EventClickDirective();
    expect(directive).toBeTruthy();
  });
});
