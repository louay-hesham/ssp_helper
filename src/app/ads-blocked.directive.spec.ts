import { AdsBlockedDirective } from './ads-blocked.directive';

describe('AdsBlockedDirective', () => {
  it('should create an instance', () => {
    const directive = new AdsBlockedDirective();
    expect(directive).toBeTruthy();
  });
});
